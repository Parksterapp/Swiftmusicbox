// Artist.swift

import Foundation

class Artist {

    // MARK: - Properties
    
    var artistId: String
    var stageName: String
    var bio: String
    var genres: [String]
    var channelVideos: [Performance]
    var ratings: [Rating]
    var bookingRequests: [BookingRequest]
    var subscriptionStatus: Bool

    // MARK: - Initialization
    
    init(artistId: String, stageName: String, bio: String, genres: [String],
         channelVideos: [Performance], ratings: [Rating], bookingRequests: [BookingRequest],
         subscriptionStatus: Bool) {
        self.artistId = artistId
        self.stageName = stageName
        self.bio = bio
        self.genres = genres
        self.channelVideos = channelVideos
        self.ratings = ratings
        self.bookingRequests = bookingRequests
        self.subscriptionStatus = subscriptionStatus
    }

    // MARK: - Additional Methods
    
    func addVideoToChannel(_ performance: Performance) {
        // Implementation details to add a performance video to the channel
        // ...
    }

    func receiveRating(_ rating: Rating) {
        // Implementation details to handle receiving a rating
        // ...
    }

    func handleBookingRequest(_ request: BookingRequest) {
        // Implementation details to handle a booking request
        // ...
    }

    func updateBio(_ newBio: String) {
        // Implementation details to update the artist's bio
        // ...
    }
}
